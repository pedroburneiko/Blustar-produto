# Persistência (Supabase). Copie para .env.local e preencha com os SEUS valores.
# .env.local é gitignored — NUNCA commite valores reais.
# A anon key é pública por design (protegida por RLS); ainda assim só por env.
# NUNCA coloque a service_role key no cliente.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
